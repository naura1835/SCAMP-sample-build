import React from "react";

import AvatarGroup from "../components/avatar-label-group/avatar-label-group.component";

export default {
  title: "components/Avatarcard",
  component: AvatarGroup,
};

export const Default = (args) => <AvatarGroup {...args} />;
